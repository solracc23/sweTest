### Login jetzt verfügbar!
Damit das Login-System korrekt nutzt, benötigt ihr Fortify.
Ihr könnt es über 'composer require laravel/fortify' und anschließend über 'php artisan fortify:install' installieren.
